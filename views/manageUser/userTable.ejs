<!--
    * 담당자 : 박신욱
    * 기능 : 이용자 관리 페이지
    * 설명 : 사용자 리스트를 초기화면, 사용자검색 조건에 따른 UI를 보여주기위한 컴포넌트
-->
<div class="table-box-wrap">
    <div class="table-box">

        <table>
            <thead>
            <tr>
                <th>사번</th>
                <th>이름</th>
                <th>부서</th>
                <th>직책</th>
                <th>메일</th>
                <th>편집 권한</th>
                <th>대여 권한</th>
                <th>열람 권한</th>
                <th>관리</th>
            </tr>
            </thead>
            <tbody id="table_data">
            <% data.map(res =>{ %>
                <tr>
                    <td><%= res.emp_no %></td>
                    <td><%= res.emp_name %></td>
                    <td><%= res.dept %></td>
                    <td><%= res.emp_position %></td>
                    <td><%= res.email %></td>
                    <td><%= res.edit_auth ? 'O' : 'X' %></td>
                    <td><%= res.rent_auth ? 'O' : 'X' %></td>
                    <td><%= res.open_auth ? 'O' : 'X'%></td>
                    <td><button onclick="manageOpen('<%= res.emp_no %>')">관리</button></td>
                </tr>
            <%})%>
            </tbody>
        </table>
    </div>
</div>
<%- include('../manageUser/manageModal.ejs') %>
